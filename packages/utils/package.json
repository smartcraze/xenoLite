{
  "name": "@repo/utils",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "echo 'No dev step for utils'",
    "build": "tsc --noEmit",
    "check-types": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@repo/tsconfig": "workspace:*"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
